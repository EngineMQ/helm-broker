apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enginemq-broker.fullname" . }}-test-connection"
  labels:
    {{- include "enginemq-broker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enginemq-broker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
